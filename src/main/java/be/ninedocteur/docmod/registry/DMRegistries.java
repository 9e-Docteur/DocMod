package be.ninedocteur.docmod.registry;

import be.ninedocteur.docmod.DocMod;
//import be.ninedocteur.docmod.client.animations.ITardisAnimation;
import net.minecraftforge.eventbus.api.EventPriority;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.event.lifecycle.FMLCommonSetupEvent;

import java.util.ArrayList;
import java.util.List;

//@Mod.EventBusSubscriber(modid = DocMod.MOD_ID, bus = Mod.EventBusSubscriber.Bus.MOD)
public class DMRegistries {
    /*public static Registry<ITardisAnimation.ExteriorAnimationEntry<?>> EXTERIOR_ANIMATIONS;



    private static List<Runnable> REGISTRIES = new ArrayList<Runnable>();

    @SubscribeEvent(priority = EventPriority.LOWEST)
    public static void init(FMLCommonSetupEvent event) {
        EXTERIOR_ANIMATIONS = new Registry<ITardisAnimation.ExteriorAnimationEntry<?>>();

        doRegisters();
    }

    public static void doRegisters() {
        for (Runnable run : REGISTRIES) {
            run.run();
        }
    }

    public static void registerRegisters(Runnable run) {
        REGISTRIES.add(run);
    }

     */

}
